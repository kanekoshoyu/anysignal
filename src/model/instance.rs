use crate::error::Result;

/// instance is generated by strategy
/// it contains multiple legs
pub trait Instance {
    /// strategy id
    fn strategy_id(&mut self) -> Result<String>;
    /// always has the capability to stop
    fn id(&mut self) -> Result<String>;
    // current status of the instance
    fn current_status(&mut self) -> Result<String>;
    // async fn that terminates the instance
    fn terminate(&mut self) -> impl std::future::Future<Output = Result<()>> + Send;
}
