# AWS S3 Configuration for Hyperliquid Historical Data
# Required for accessing Hyperliquid's S3 archive bucket containing historic asset context data
# Documentation: https://hyperliquid.gitbook.io/hyperliquid-docs/historical-data

# Your AWS Access Key ID - obtain from AWS IAM console
# Must have S3 read permissions for the hyperliquid-archive bucket
AWS_ACCESS_KEY_ID=your_access_key_here

# Your AWS Secret Access Key - obtain from AWS IAM console
# Keep this secret and never commit it to version control
AWS_SECRET_ACCESS_KEY=your_secret_access_key_here

# AWS Region where the Hyperliquid S3 bucket is located
# Default: ap-northeast-1 (Asia Pacific - Tokyo)
AWS_REGION=ap-northeast-1

# S3 bucket name containing Hyperliquid historical data
# Default: hyperliquid-archive (official Hyperliquid public bucket)
HYPERLIQUID_S3_BUCKET=hyperliquid-archive

# QuestDB Configuration
# ILP/HTTP ingress address — host:port (QuestDB HTTP port, default 9000)
QUESTDB_ADDR=questdb.bounteer.com:9000

# Optional: QuestDB credentials — only needed if authentication is enabled
# QUESTDB_USER=admin
# QUESTDB_PASSWORD=quest
